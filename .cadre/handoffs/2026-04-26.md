## 2026-04-26 — Architectural simplification: tracked operational state, CC native worktrees

### Narrative

Session opened with the small loose end from prior: start the handoff-mx-cadre agent (TODO #12). User immediately surfaced friction with the worktree workflow — the harness resets cwd to the primary working directory after every Bash call, which breaks the "stay in one Claude session and hop worktrees" mental model. Two felt gaps emerged from that single ergonomic complaint: (1) one-Claude-session-per-worktree is the only viable convention; (2) `.cadre/` was gitignored, so fresh worktrees started blank.

The conversation pivoted into solving the gitignore-vs-worktree problem first. Initial design: a "playbook" — `cadre/playbook.json` listing operational-state files; a worktree-init script copying them in; a PostToolUse bidirectional sync hook keeping all worktrees + main byte-identical. Built and committed (commit `1345135`) on `feat/playbook-cadre`. Never merged.

User then shared a Reddit post on `.claude/` structure scaling and asked to pivot scope to include `.claude/` restructure. Invoked `/plan-cadre` for a unified rework plan. The plan-cadre brainstorm exposed a sequence of progressively simpler approaches: the user's intent (`abandon canonical store, just keep in main + use worktreeinclude`) led to a sharp clarifying question — *"won't merging propagate the edits?"* — which surfaced the load-bearing fact: for `git merge` to propagate, files must be tracked. The privacy concern that drove the gitignore was reconsidered. Operational state isn't actually sensitive (lab-notebook category); it just felt private. User upgraded "track playbook subset" to "track everything in `.cadre/`" mid-conversation.

Parallel discovery via claude-code-guide: **Claude Code already ships native worktree support.** `claude --worktree <name>` creates the worktree, copies gitignored files via `.worktreeinclude`, launches the session, auto-cleans on exit. `WorktreeCreate`/`WorktreeRemove` hooks are real harness events. Subagent worktree isolation via `isolation: worktree` frontmatter. The custom shell wrapper and PostToolUse sync hook we'd built were redundant with first-class harness features.

The architecture collapsed dramatically. The full bidirectional sync + custom command + worktree-init script + JSON config + `.worktreeinclude` config + per-worktree SCRATCH.md all evaporated. What remained: un-gitignore `.cadre/` and `CLAUDE.md`, adopt CC native, update doctrine. The playbook concept survived for less than 24 hours.

Brooks reflection identified five candidate shifts; the load-bearing one was scope split — ship the simplification (PR #1) separately from the anticipatory `.claude/` Reddit-style restructure (PR #2 deferred). The other four (branch rename, CC native verification, namespace cleanup, pre-tracking scrub) folded into PR #1 detail or were captured as future work.

Execution: reset `feat/playbook-cadre` to `origin/main` (drops `1345135`); pre-tracking scrub (one personal email redacted from bootstrap plan; nothing else sensitive); un-gitignore `.cadre/` + `CLAUDE.md`; commit; CLAUDE.md doctrine update (Worktrees entry rewritten for CC native, "The playbook" entry replaced with "Operational state is tracked"); branch renamed to `refactor/track-operational-state`; pushed; PR #4 opened. Live-fire verification in a fresh worktree (handed off to a separate Claude instance) revealed one drift surprise — `claude --worktree` branches from `origin/HEAD`, not from a specified PR — but the architecture itself verified clean. PR #4 merged as `8570e36`.

Workflow surprise worth carrying forward: the test plan I wrote conflated "spawn fresh work" with "verify a PR's content." Those are different flows. The verifying instance fast-forwarded its worktree to the PR head; the recipe is now in the doctrine.

### Subsequent same-day refinements (after PR #4 merged)

After landing the load-bearing simplification (PR #4, `8570e36`), the day continued with four smaller PRs that closed gaps surfaced by dogfooding the new workflow:

- **PR #5 — session housekeeping** (merged as `e524507`). Captured this session's narrative in handoff, marked TODO #27 DONE (Playbook → superseded), added TODO #28, captured ADRs 063–067, refined CLAUDE.md doctrine note that `claude --worktree` branches from `origin/HEAD` (gap surfaced by PR #4's verification).
- **PR #6 — `.claude/` Reddit-style scaffold** (merged as `7b1166c`). Closed TODO #28: scaffolded `.claude/{rules, hooks, commands, agents}/` empty with `.gitkeep`; added `*.local.md` / `*.local.json` to `.gitignore`; CLAUDE.md doctrine entry "**`.claude/` directory shape**" added. Removed stale `.claude/references/`. Content migration from CLAUDE.md to `rules/` deferred (no crowding yet); namespace cleanup `.cadre/references/` → `.cadre/references/` deferred (different content categories). ADR-068.
- **PR #7 — branch-default doctrine refinement** (merged as `1b4b43b`). User caught that the new "Worktrees" doctrine read as "use worktrees for every change," which wasn't the intent. Refined to "Default flow: branch + PR" leading; "Worktrees: parallel Claude sessions" as the called-out exception. ADR-069 (no supersession of ADR-065 — the worktree spawn mechanism is unchanged; only the framing of *when* to reach for them).
- **PR #8 — auto-merge mechanics** (this PR). Reconciled ADR-010 (no auto-merge; human decides) with the user's "checks fire and merge happens" workflow: human stays the decision-maker; orchestrator enables `gh pr merge --auto` once human says yes; gates fire; merge lands on green. ADR-070 captures the operational mechanic; ADR-010 unchanged in substance, cross-linked. TODO #14 updated (merge review flow + decision-maker note).

### Decisions this session (condensed — full entries in `.cadre/logs/ADR/decision-log.md`)

Session decisions span ADR-063 through ADR-070:

1. **ADR-063:** Bidirectional sync architecture rejected before merge.
2. **ADR-064:** `.cadre/` and `CLAUDE.md` un-gitignored; tracked operational state replaces playbook concept. Supersedes ADR-014.
3. **ADR-065:** Adopt CC native worktree primitives. Supersedes ADR-021.
4. **ADR-066:** Reddit-style `.claude/` restructure deferred to PR #2 (originally TODO #28; closed in PR #6).
5. **ADR-067:** SCRATCH.md concept abandoned.
6. **ADR-068:** `.claude/` Reddit-style scaffolding adopted (TODO #28 closed via PR #6).
7. **ADR-069:** Branches are the default flow; worktrees for parallel Claude sessions only.
8. **ADR-070:** Auto-merge mechanics (`gh pr merge --auto` after human decides yes); cross-linked to ADR-010.

### Open threads / next-up

**Active task board:** `.cadre/todos.md` now has 28 items. Completed this session: TODO #27 (the Playbook, DONE — superseded by tracking) and TODO #28 (`.claude/` Reddit-style scaffold, DONE via PR #6).

**Original target still unaddressed: TODO #12 (handoff-mx-cadre agent).** The session opened with this as the goal but pivoted to architecture work for the entire day. **Now genuinely next-up — and the user has signaled a fresh session will start with this work.** The workflow infrastructure is finally clean enough to build on. The agent will run autonomously to maintain `.cadre/handoff.md` (live mutation during session, session-end stamp, session-start surfacing, lifecycle/archive). Now plausibly the simplest version of itself since handoff is just a tracked file edited via standard tools.

**Pending implementation:**
- **TODO #12 — handoff-mx-cadre agent. NEXT (new session).** Now plausibly the simplest version of itself, since handoff is just a tracked file edited via standard tools.
- **TODO #61 (ADR-061) — researcher-cadre format migration** (skill → agent).
- **TODO #14 — three-review architecture detail.** Now incorporates auto-merge mechanics (ADR-070) once CI/swarm/risk-detection layers ship.
- **TODO #22 — base CI workflow.** Required before auto-merge mechanics activate in practice.

**Pointers for next session (handoff-mx-cadre):**
- Workflow infrastructure is clean: tracked operational state, branch + PR default, CC native worktrees for parallel sessions, auto-merge mechanics specified.
- Five PRs landed today: #4 (architectural simplification), #5 (housekeeping), #6 (`.claude/` scaffold), #7 (branch-default doctrine), #8 (auto-merge mechanics).
- Plan that drove the architectural pivot: `.cadre/plans/what-di-you-think-floofy-rose.md`.
- Workflow surprise carrying forward: `claude --worktree` branches from `origin/HEAD`, not from a specified PR. To verify a PR's content, fast-forward the worktree's branch to the PR head after creation. Doctrine notes this.
- Original handoff-maintainer contract (TODO #12): live mutation during session, session-end stamp, session-start surfacing, lifecycle/archive. User never has to prompt it. Per-commit narrative lives in commit messages, not handoff.

### Pointers

- **Repo:** `github.com/WolfOnWings/cadre` (public). PRs through #8 merged.
- **Tracked operational state:** `.cadre/` and `CLAUDE.md` now tracked. Standard git merge propagates edits.
- **Plan:** `.cadre/plans/what-di-you-think-floofy-rose.md` (this session's planning artifact).
- **Doctrine:** `CLAUDE.md`. Updated this session: Worktrees entry (CC native + branch-default framing), "Operational state is tracked" replaces "The playbook" entry, "`.claude/` directory shape" entry added.
- **Decision log:** ADR-063 through ADR-070 captured this session.

---

## 2026-04-26 — handoff-mx integration flow exercised

### Narrative

This session was a minimal probe of the handoff-mx-cadre integration loop itself — not a content session. The events log captured a single user prompt (a bare `.`) and one corresponding orchestrator action (a `Bash` listing of `.cadre/` to verify state). The prompt's likely intent was to trigger the SessionStart flow so the integration subagent would consume any pending events and produce a fresh handoff entry against the prior day's now-stale state.

The prior session (2026-04-26 architectural simplification) had landed the load-bearing workflow rework — tracked operational state, CC-native worktrees, branch+PR default, auto-merge mechanics — across PRs #4–#8. It explicitly named TODO #12 (handoff-mx-cadre agent) as next-up. Since then, two further commits had landed on main: `7950640` adding `bypassPermissions` to the agent's frontmatter (a hook-permission fix surfaced during live-fire), and `92a649d` capturing ADR-073 plus stale-doc fixes from the same live-fire findings. Both commits are evidence the handoff-mx subagent was already shipped and being exercised against the real loop — this session is a continuation of that exercise rather than a fresh initiation.

The integration itself ran clean: events log (126 bytes, 2 entries) renamed to `.processing-<ts>`; both prior `.cadre/handoff.md` entries (2026-04-26 architectural simplification, 2026-04-23→24 skill ecosystem buildout) archived under `.cadre/handoffs/`; new entry composed; atomic rename; consumed events deleted. No collisions in the archive directory (the `2026-04-26.md` archive did not previously exist — confirming the prior architectural-simplification entry had remained at handoff root since merge of PR #5 / #6 housekeeping). One untracked artifact noted in the handoffs directory: `backup_handoff` — outside the integration footprint, left untouched.

The session illustrates the steady-state shape of the handoff-mx flow: when a session does almost nothing, the integration entry should reflect that honestly rather than confabulate substance. The 200-line Narrative budget is a ceiling, not a target. The real signal carried forward from this session is structural — the integration loop closed without manual intervention against a non-trivial pre-existing handoff state (two entries totaling ~26 KB).

### Decisions

No new ADRs this session. No formal decisions taken — the session content was the integration mechanic exercising itself.

### Active Items

- **TODO #12 — handoff-mx-cadre agent.** Subagent is shipped and now demonstrably running against real session boundaries (this entry is its product). Live-fire findings landed in commits `7950640` (bypassPermissions frontmatter fix) and `92a649d` (ADR-073 + stale-doc fixes) since the prior handoff entry was written. Status of TODO #12 itself depends on whether the per-prior-handoff scope (live mutation during session, session-end stamp, session-start surfacing, lifecycle/archive) is fully covered by what shipped — verify against `.cadre/todos.md` next session.
- **TODO #61 (ADR-061) — researcher-cadre format migration** (skill → agent). Untouched.
- **TODO #14 — three-review architecture detail.** Untouched; awaits CI/swarm/risk-detection layers.
- **TODO #22 — base CI workflow.** Untouched; gating prerequisite for auto-merge mechanics activating in practice.
- **Untracked artifact:** `.cadre/handoffs/backup_handoff` exists outside the date-keyed archive chain. Origin unclear; review and either rename to a dated archive or delete next session.
- **ADR-073** referenced in commit `92a649d` — captured per the live-fire findings on handoff-mx. Read `.cadre/logs/ADR/decision-log.md` for the full record before next handoff-mx work.

### Changes

No code or doctrine changes this session. Filesystem effects from the integration itself:

- `.cadre/handoff.md` — REPLACED with this single new entry; prior two entries moved to archive.
- `.cadre/handoffs/2026-04-26.md` — created; archives the prior 2026-04-26 architectural-simplification entry verbatim.
- `.cadre/handoffs/2026-04-23.md` — created; archives the prior 2026-04-23→24 skill-ecosystem-buildout entry verbatim.
- `.cadre/session-events.log.processing-<ts>` — created (consume), then deleted (completion).
- `.cadre/session-events.log` — fresh empty file on next event write by logger.

---

## 2026-04-26 — handoff-mx-cadre shipped (TODO #12 closed)

*Hand-rolled by the orchestrator at end of session because this session predated the handoff-mx-cadre merge — settings.json wasn't loaded, so no events were captured by the logger. Archived here to preserve the substantive narrative of the TODO #12 ship.*

### Narrative

Session opened with TODO #12 (handoff-mx-cadre agent) named as next-up from the prior handoff. Before pivoting to the main work, the session caught a calibration drift along the way: PR #9 added TODOs #29 and #30 (break out CLAUDE.md into `.claude/` primitives + document rules/commands conventions) via the full branch+PR ceremony, which the user flagged as overkill for queueing future work. PR #10 captured the operational-metadata exception as ADR-071 — direct-to-main allowed for `.cadre/todos.md`, `.cadre/handoff.md`, ADR log; branch+PR remains default for everything else, including doctrine changes (the ADR itself shipped as a PR).

`/plan-cadre` invoked for the main work. The brainstorm went through four Brooks loop-backs — each pivot resolving a constraint surfaced by verification or user calibration:

1. **Initial Will:** lifecycle hooks (SessionStart / Stop / SessionEnd) + CLAUDE.md inline-maintenance posture, no subagent. Rejected for orchestrator context-bloat, manual discipline required, no clean separation of concerns. Verification surfaced that Stop hook has no `additionalContext` field (only heavy `decision: block`) — UserPromptSubmit is the actual per-turn injection mechanism.

2. **Second Will:** logger hook captures events; threshold-triggered subagent dispatched mid-session for live mutation. User pivoted back to subagent architecture once a structured events log emerged as a way to give the subagent deterministic input (solving the "no subagent because re-briefing overhead" objection from the first round).

3. **Third Will (Shape D, ADR-072):** boundary-synthesizer. Logger fires on UserPromptSubmit / PostToolUse / Stop (fast, non-blocking); synthesizer agent dispatched at SessionStart and SessionEnd via `"type": "agent"` hook config. Verified live that agent hooks block synchronously — hence boundary-only triggering rather than mid-session threshold (visible 5–30s pauses every ~5min would be intolerable). User caught two refinements during planning: progressive-disclosure summarization is a failure step (handoff is already the compressed surface; further compression at consumption time = double-loss); CLAUDE.md doctrine entry isn't needed because handoff mechanics are entirely worker-side, no orchestrator discipline required.

4. **Fourth Will (final shipped, ADR-073):** orchestrator-dispatched. Two consecutive live-fire tests confirmed `"type": "agent"` hook config silently fails to dispatch in current CC build — at SessionEnd (documented restriction; only `command`/`mcp_tool` supported there) AND at SessionStart (per GitHub Issue #39184 — agent hooks reportedly fail across SessionStart, UserPromptSubmit, PostToolUse). User cut through the workaround spiral: instead of fighting the hook→agent dispatch, have the orchestrator dispatch the subagent itself via the Agent tool as its first action on session start. The SessionStart hook becomes a thin command that injects an orientation instruction via `additionalContext` telling the orchestrator to dispatch the subagent and read the synthesized handoff before responding to the user.

The shipped architecture: `.claude/hooks/handoff-mx-logger-cadre.ts` (TS-on-Bun, registered on UserPromptSubmit / PostToolUse / Stop) appends NDJSON events to `.cadre/session-events.log` non-blocking. `.claude/hooks/handoff-mx-prime-cadre.ts` (registered on SessionStart with matchers `startup|resume|clear|compact`) emits the orientation instruction. `.claude/agents/handoff-mx-cadre.md` defines the synthesizer subagent (subagent mode; 9-step SOP; declared I/O footprint; 7 anti-patterns; `permissionMode: bypassPermissions` for clean dispatch). The orchestrator dispatches the subagent on first turn; subagent renames events log to `.processing-<ts>` (atomic consume), reads it + handoff, archives prior entries to `.cadre/handoffs/<entry-date>.md`, writes new four-section entry as sole content of `.cadre/handoff.md`, deletes the `.processing-<ts>` file.

Live-fire test verified the architecture end-to-end. 17 events from 3 test sessions integrated into a synthesized four-section entry; 2 prior entries archived; crash-recovery branch exercised in practice (a `.processing-*` from a partially-failed prior run was caught and consumed cleanly). First-run cost ~8min against the 26KB existing handoff; steady-state runs against single-session input should be substantially faster.

Two empirical CC harness findings worth carrying forward as design constraints: `"type": "agent"` hooks silently fail at SessionStart, UserPromptSubmit, PostToolUse, and SessionEnd in current build. Use `"type": "command"` hooks for deterministic work; for LLM-judgment work at hook trigger points, the verified pattern is "hook injects an orientation instruction; orchestrator dispatches the subagent via Agent tool." Doc/build mismatch documented in GitHub Issue #39184.

User calibrations carried forward: (a) plan-cadre's pure question-mode posture over-rotates against collaboration — surface options with my opinion, ask user to react, don't interrogate; (b) integrating research findings into design wholesale is a process failure — surface findings for user judgment first; (c) when verification shows a clear blocker, name the problem cleanly and stop iterating workarounds; (d) operational-metadata exception (ADR-071) is for `.cadre/` files specifically — `.gitignore` and other root-level infra still use branch+PR (caught when a direct-to-main push to `.gitignore` got rejected by branch protection).

### Decisions

- **ADR-071** — Operational-metadata exception to branch+PR default. Trivial mutations to `.cadre/todos.md`, `.cadre/handoff.md`, or the ADR log can land directly on main; branch+PR remains the default for code, behavior, or doctrine changes (including this ADR itself).
- **ADR-072** — handoff-mx-cadre boundary-synthesizer architecture (Shape D). Logger captures events; synthesizer dispatched by lifecycle hooks at session boundaries; events-log atomic consume via rename; archival to `.cadre/handoffs/<date>.md`; handoff.md holds one current entry. *SUPERSEDED BY ADR-073 (dispatch mechanism only — boundary-synthesizer intent preserved).*
- **ADR-073** — Synthesizer dispatch shifts to orchestrator (hook→agent broken in CC build). The orchestrator (main session) dispatches the synthesizer subagent via the Agent tool as its first action on session start; SessionStart hook is a thin command that injects an orientation instruction via `additionalContext`. Cites GitHub Issue #39184 for the underlying CC bug. Verified end-to-end via live-fire test.

### Active Items

- **TODO #12** — handoff-mx-cadre agent: **DONE** (ADR-072 / ADR-073).
- **TODO #29** — Document `.claude/rules/` and `.claude/commands/` conventions (prerequisite for #30). Open.
- **TODO #30** — Break out CLAUDE.md into `.claude/` primitives. Gated on #29.
- **TODO #31** — handoff-mx events-log filtering / sampling. Added this session per live-fire observation (88KB log from one test session). Not blocking v1; revisit if synthesis runtime becomes a UX issue at steady state.
- **TODO #14** — Three-review architecture detailed design. Now incorporates ADR-070 auto-merge mechanics; depends on TODO #22 (CI) and stranger-swarm implementation.
- **TODO #22** — Base CI workflow. Required before auto-merge mechanics activate in practice.
- **TODO #61 / ADR-061** — researcher-cadre format migration (skill → agent). Underway-but-not-started.
- **First-deployment verification (next session, NEXT)** — first live SessionStart fire of the new architecture against the main repo. Watch for: priming instruction injection, orchestrator dispatch on first turn, synthesizer cost on the current 26KB handoff (~8min expected based on test data), four-section output quality, archival correctness. If anything fails, this backup file is the fallback.

### Changes

- **New primitives:**
  - `.claude/agents/handoff-mx-cadre.md` — synthesizer subagent contract (215 lines; 9-step SOP; PRISM-aligned identity; 7 anti-patterns; declared I/O footprint).
  - `.claude/hooks/handoff-mx-logger-cadre.ts` — event logger (TS-on-Bun; UserPromptSubmit / PostToolUse / Stop / SessionEnd cases; 4096-char field truncation; non-blocking on errors).
  - `.claude/hooks/handoff-mx-prime-cadre.ts` — SessionStart orientation injector (returns `additionalContext` with dispatch instruction; non-blocking).
  - `.claude/settings.json` — hook registrations: SessionStart (prime), UserPromptSubmit / PostToolUse / Stop (logger).

- **Plan + research:**
  - `.cadre/plans/ok-let-s-finally-start-pure-dusk.md` — full plan-cadre output (intent → could → should → parked → will → Brooks → architect findings → action plan → subagent draft → hook script drafts → settings).
  - `.cadre/research/2026-04-26-handoff-mx-prior-art.md` — researcher-cadre brief on session-state continuity prior art (Anki / LSP / memex / agent-systems / OS / IDE).

- **Doctrine + decisions:**
  - `CLAUDE.md` — operational-metadata exception clause appended to "Default flow: branch + PR" entry.
  - `.cadre/logs/ADR/decision-log.md` — ADRs 071, 072, 073 added; ADR-072 cross-linked SUPERSEDED BY ADR-073.

- **Task board:**
  - `.cadre/todos.md` — TODOs #29, #30 added (PR #9), #31 added (this session, end of session); TODO #12 marked DONE.

- **Infrastructure:**
  - `.gitignore` — added `.cadre/session-events.log`, `.cadre/session-events.log.processing-*`, `.cadre/logs/hooks/*.log`, `.claude/worktrees/`.
  - `.claude/agents/.gitkeep`, `.claude/hooks/.gitkeep` — deleted (real files now occupy the dirs).

- **PRs landed this session:**
  - **PR #9** — `docs(todos): queue claude.md → .claude/ migration (#29, #30)`
  - **PR #10** — `docs(claude.md): operational-metadata exception to branch+PR default (ADR-071)`
  - **PR #11** — `feat(handoff-mx): handoff-mx-cadre synthesizer subagent + lifecycle hooks` (the main ship; multiple fix-up commits during live-fire testing covering top-level await fix, SessionEnd→logger, SessionStart→orchestrator-dispatch, agent permissionMode)
  - **PR #12** — `chore(gitignore): ignore hook logs and worktrees dir`

### Pointers

- **Repo:** `github.com/WolfOnWings/cadre` (public). Main at `6e4bdd3` after this session.
- **Plan:** `.cadre/plans/ok-let-s-finally-start-pure-dusk.md` (full Brooks loop-back trail).
- **Research:** `.cadre/research/2026-04-26-handoff-mx-prior-art.md`.
- **Decision log:** ADRs 071–073 added this session; ADR-072 marked SUPERSEDED BY ADR-073.
- **Subagent contract:** `.claude/agents/handoff-mx-cadre.md` (source of truth for what the synthesizer does at runtime).
- **Hook scripts:** `.claude/hooks/handoff-mx-{logger,prime}-cadre.ts`.
- **Hook registrations:** `.claude/settings.json`.
- **GitHub Issue #39184** — empirical CC bug we hit twice; agent hooks silently fail across SessionStart/UserPromptSubmit/PostToolUse. Worth tracking upstream.

### User signals to carry forward

- All bootstrap-era + 4/23 + 4/26 signals still apply (5/10 yanking, ping-pong cadence, "we don't know yet" is valid, corrections at earned scope, defer/TBD as achievements, follow asides, "we" voice, PB&J granularity test, vocabulary effectiveness test, worktree the moment tracked-territory work begins).
- **plan-cadre over-rotates to question-mode** — at calibration moments where the user has signaled exploration, surface options with my opinion + invite reaction; don't interrogate. This session repeatedly drifted into pure-question facilitation that the user explicitly rejected ("a bit much," "leans too much towards me doing everything").
- **Research integration is collaborative, not wholesale** — when researcher-cadre returns findings, surface them to user for adopt/reject judgment per pattern, not absorb them all into the design unannounced. Caught this turn with progressive-disclosure specifically and the other findings collectively.
- **Empirical reality > documentation when they conflict** — the agent-hook failure was hidden by a confident first verification that said it was supported. The second verification + GitHub Issue #39184 + two live-fire failures together painted the truer picture. Trust verification cycles, not single doc fetches.
- **Operational-metadata exception is narrow** — `.cadre/todos.md`, `.cadre/handoff.md`, ADR log only. Root files (`.gitignore`, `CLAUDE.md`) and `.claude/` infrastructure still use branch+PR (branch protection will reject direct pushes regardless).

---

## 2026-04-26 — handoff-mx self-recursive integration probe

### Narrative

This session was a recursive exercise of the handoff-mx-cadre flow against itself. The user issued a single bare prompt (`.`) — the now-conventional "trigger SessionStart and let the machinery run" signal. The orchestrator-side response was unusual: rather than dispatch the integration subagent first and wait, it ran the full integration SOP inline (Bash-listed `.cadre/`, renamed `session-events.log` → `.processing-<ts>`, archived both prior entries to `.cadre/handoffs/2026-04-26.md` and `2026-04-26.md` [sic — note the same-date collision foreshadowing], wrote a new `handoff.md` describing the session, then atomically renamed it). After the inline integration closed, the orchestrator dispatched the handoff-mx-cadre subagent (this run) as a belt-and-suspenders pass, then immediately hit Stop. The dispatched subagent therefore found a freshly-integrated `handoff.md` describing the very session whose events the orchestrator-inline run had already consumed.

The events log presented to me on this second pass contained the orchestrator's inline integration run — every Bash, Read, Write, and the dispatch-and-Stop sequence. There was no further user content; nothing semantic happened beyond the integration mechanic exercising itself twice (once inline, once via subagent).

The right play was to honor what actually occurred: archive the orchestrator-written entry to `.cadre/handoffs/2026-04-26.md` (which already held the architectural-simplification entry from earlier in the day, requiring an append-with-separator), and replace `handoff.md` with a single new entry describing the recursive-probe shape. Multi-entry-same-date is handled per SOP — read existing archive content, append new entry after `---`, byte-compare the pre-existing 9517 bytes for immutability. Verification passed (md5 match on the head portion).

The structural finding worth carrying: when the orchestrator runs the integration inline AND dispatches the subagent, the subagent runs against the integration's *output* rather than its input — a degenerate self-handoff loop. The current SessionStart wiring should pick exactly one of these flows. ADR-073 (committed `92a649d`, prior session) is the most recent landed thinking on handoff-mx live-fire findings; check whether it already addresses this and, if not, file the next-up ADR. Alternative resolutions are: (a) SessionStart dispatches the subagent ONLY (orchestrator never integrates inline); (b) the subagent detects "events already consumed" via the empty-log fast-path and returns no-op; (c) the orchestrator's first-action-on-session-start is replaced by the subagent dispatch, removing the inline path entirely.

The 200-line Narrative budget is far over-budget for this session's actual signal — kept short on principle. Steady-state shape: when the session content IS the mechanic, write the mechanic; do not invent substance.

### Decisions

No new ADRs this session. Possible follow-up ADR (next session): clarify single-path SessionStart wiring to prevent orchestrator-inline + subagent-dispatch double execution. Cross-reference ADR-073 in `.cadre/logs/ADR/decision-log.md` first.

### Active Items

- **TODO #12 — handoff-mx-cadre agent.** Subagent demonstrably runs end-to-end; live-fire surfaced a new failure mode (orchestrator-inline + subagent-dispatch double-execute against the same events). Next session: (1) read ADR-073 to confirm scope; (2) audit `.claude/hooks/handoff-mx-prime-cadre.ts` (or equivalent priming hook) to see whether it instructs the orchestrator to dispatch-only or integrate-inline; (3) settle the path and commit a fix or new ADR.
- **TODO #61 (ADR-061) — researcher-cadre format migration** (skill → agent). Untouched.
- **TODO #14 — three-review architecture detail.** Untouched; awaits CI/swarm/risk-detection layers.
- **TODO #22 — base CI workflow.** Untouched; gating prerequisite for auto-merge mechanics activating in practice.
- **Untracked artifact:** `.cadre/handoffs/backup_handoff` still present (carried from prior session). Origin unclear; review and either rename to a dated archive or delete next session.
- **ADR-073** referenced in commit `92a649d` — still the authoritative live-fire findings record; consult `.cadre/logs/ADR/decision-log.md` before further handoff-mx work.

### Changes

No code, doctrine, or product changes this session. Filesystem effects from this integration pass:

- `.cadre/handoff.md` — REPLACED with this single new entry; the orchestrator-written entry archived.
- `.cadre/handoffs/2026-04-26.md` — APPENDED with the orchestrator-written `handoff-mx integration flow exercised` entry, separated from the pre-existing `Architectural simplification` entry by `---`. Pre-existing 9517-byte portion verified byte-identical (md5 match).
- `.cadre/session-events.log.processing-20260426T215727Z` — created (consume), deleted (completion).
- `.cadre/session-events.log` — fresh empty file on next event write by logger.


---

## 2026-04-26 — backup_handoff archived; double-dispatch loop named

### Narrative

This session is one half of a two-session arc that finally closed out the handoff-mx-cadre boot phase. The events log presented to me spans two consecutive Claude sessions on the same date: an earlier session (`8bed9fe8…`) where the user wanted to compare the synthesizer's output against a manually-maintained `backup_handoff` file, and a brand-new session (`483571bd…`) opened with the conventional bare `.` prompt that triggered me. From this synthesizer's seat both sessions are one continuous arc — the prior session's tail fed directly into mine.

Earlier session opened with the user proposing a side-by-side diff between the synthesizer's freshly-written `handoff.md` ("handoff-mx self-recursive integration probe") and the orchestrator's hand-rolled `backup_handoff` from the previous day. The orchestrator misread the proposal — it copied `backup_handoff` to a `.preserve.<rand>` file as if to begin reading it. User cut in immediately: "No don't peek. We'll wait." The Stop event landed before any comparison happened. The signal there is delicate: the user wanted to *contrast* the two artifacts in their unmodified state, not have the orchestrator pre-load one. Honor "we'll wait" as a literal instruction; don't rush to be helpful.

The user came back with a clearer disposition: "just archive the backup. My thinking is that the handoff agent just didn't have all the context. The session logger should be running from the beginning now and the session after this one should receive a proper handoff." That is the load-bearing acceptance of this whole arc — the synthesizer's sub-optimal first run was *expected*, because the prior session predated logger activation, so there were no events to synthesize from. The synthesizer is not broken. It just had nothing to work with. The fix is structural and self-healing — once the logger runs from session genesis, subsequent runs have real input, and the artifact quality climbs without further intervention.

The orchestrator then archived `backup_handoff` as a third entry inside `.cadre/handoffs/2026-04-26.md`, separated from the two existing entries by `---`, with a one-paragraph italic preamble explaining the hand-rolled provenance. It then deleted the loose `backup_handoff` file. Mechanically this used `Edit` rather than the SOP's read-content / append-after-separator dance — pragmatic and correct, but worth flagging that archive append was done outside the synthesizer's footprint. The user's "YEah that took forever" closed that session; long Edit on a 13KB file with a 9KB old_string is a known cost.

The new session then opened with `.` — minute-and-a-half later. Logger fired from session start. I dispatched normally and found a reasonable pre-existing state: a single fresh entry in `handoff.md` (the prior session's self-recursive probe writeup), and the 2026-04-26.md archive holding three entries (architectural simplification → handoff-mx integration flow exercised → hand-rolled handoff-mx-cadre shipped). My own startup Bash calls — `ls /c/Users/duham/Projects/cadre/.cadre/session-events.log*` and `ls -la /c/Users/duham/Projects/cadre/.cadre/` — appended to the events log before I performed the atomic-rename consume, so they appear at the tail of the events I'm now synthesizing. That's expected per the SOP (logger writes to a fresh log post-rename); noting it for transparency.

The structural finding from the prior synthesizer run worth carrying forward, restated in light of what we learned this session: the prior synthesizer's "self-recursive integration probe" entry surfaced concern about a double-dispatch loop (orchestrator runs integration inline AND dispatches synthesizer, so synthesizer processes events describing the integration that just happened). The actual mechanism is now clearer. There is no "orchestrator runs integration inline" — what happened in the still-earlier session was that the orchestrator ran an *ad-hoc* integration mechanic before the synthesizer existed in shipped form. The synthesizer-as-shipped does not have a competing inline path. The remaining concern collapses to: the synthesizer's *own* tool calls during integration appear in the logger's events log for the next session to consume. That is by design (SOP Step 1: rename log to `.processing-<ts>` *before* doing work; logger writes after that point go to a freshly-created log and represent next-session input). The prior synthesizer run was confused about which session's events it was looking at — looking at *its own* events log entries from the consume action and reading them as a different session's content. That's a self-referential confusion, not a structural double-dispatch. The next iteration of TODO #12 should add an SOP clarification: "events generated by the synthesizer's own tool calls land in the freshly-created log post-rename and represent next-session input; do not reinterpret them as same-session content."

User signal worth preserving: the empirical lesson the user named explicitly — "the handoff agent just didn't have all the context. The session logger should be running from the beginning now" — is the genesis-condition principle. Synthesizer quality is bounded above by logger coverage, not by SOP sophistication. Steady-state quality requires logger active from the first user prompt of the session being summarized. This was the gap that delayed TODO #12's effective close to today (2026-04-26), and it's now closed.

The 200-line Narrative budget was over-budget for this session's actual signal but under-budget for the previous session's, since the previous session was the substantive one. Wrote at the right scale for the combined arc.

### Decisions

No new ADRs this session. Possible follow-up captured under TODO #12: clarify in the synthesizer SOP that post-consume events represent next-session input (eliminating the self-referential-confusion failure mode the prior run exhibited). Read `.cadre/logs/ADR/decision-log.md` for the ADR-073 / ADR-072 lineage before any new ADR work — they remain authoritative on the dispatch architecture.

### Active Items

- **TODO #12 — handoff-mx-cadre agent.** DONE per ADR-073 (synthesizer + logger + prime hook shipped, end-to-end verified). Two refinement sub-items surfaced this arc and not yet captured in TODOs: (a) add SOP clarification on post-consume events as next-session input, to prevent the self-referential-confusion failure mode the prior synthesizer run exhibited; (b) consider whether to swap the orchestrator's archive-append `Edit` flow (used for the backup_handoff archival earlier) for the synthesizer's read-then-append pattern in any future hand-rolled archive operation. Both small; both fit in the next session's first 30 minutes if the user opts to address them.
- **TODO #61 / ADR-061 — researcher-cadre format migration** (skill → agent). Untouched.
- **TODO #29 — Document `.claude/rules/` and `.claude/commands/` conventions.** Untouched. Prerequisite for #30.
- **TODO #30 — Break out CLAUDE.md into `.claude/` primitives.** Untouched. Gated on #29.
- **TODO #31 — handoff-mx events-log filtering / sampling.** Untouched. Logged this session's events log was 53KB; well under prior 88KB observation. Not yet a UX issue at steady state; revisit if synthesis runtime climbs.
- **TODO #14 — Three-review architecture detail.** Untouched; awaits CI + stranger-swarm + risk-detection layers.
- **TODO #22 — Base CI workflow.** Untouched; gating prerequisite for auto-merge mechanics activating in practice.
- **No untracked archive artifacts.** `backup_handoff` was archived this session; the prior session's "untracked artifact" Active Item is now resolved — `.cadre/handoffs/` contains only date-keyed archive files (`2026-04-22.md`, `2026-04-23.md`, `2026-04-26.md`).
- **User-named expectation:** "the session after this one should receive a proper handoff." This entry is that handoff. Next session is the proof point — first synthesizer run with logger active from session genesis on a substantive content session will tell us whether the architecture delivers in steady state.

### Changes

No code, doctrine, or product changes this session. Filesystem effects from this integration pass plus the prior session's archive operation:

- `.cadre/handoffs/2026-04-26.md` — APPENDED with a fourth entry (this session's predecessor's "self-recursive integration probe" writeup), separated by `---`. Pre-existing 26843-byte portion verified byte-identical (md5 match). File now holds four entries totaling 31912 bytes: architectural simplification → handoff-mx integration flow exercised → handoff-mx-cadre shipped (hand-rolled) → handoff-mx self-recursive integration probe.
- `.cadre/handoffs/backup_handoff` — REMOVED earlier this arc by the orchestrator after its substantive content was archived as the third entry in `2026-04-26.md`. (Prior session footprint, noted here for completeness.)
- `.cadre/handoff.md` — REPLACED with this single new entry; the prior "self-recursive integration probe" entry archived as noted above.
- `.cadre/session-events.log.processing-2026-04-26T22-07-58` — created (consume), then deleted (completion).
- `.cadre/session-events.log` — fresh empty file on next event write by logger.
