.env*
*.local.md
*.local.json

# Bun
node_modules/
.tsbuildinfo

# Tool caches
.fallow/
coverage/

# handoff-mx-cadre operational state (events stream + per-hook debug; all transient)
.cadre/logs/handoff-mx/

# task-mx-cadre operational state (per-hook debug + flag files; all transient)
.cadre/task-mx/logs/

# Stale log artifacts from earlier hook iterations (transient debug; not source).
.cadre/logs/hooks/
.cadre/session-events.log

# Git worktrees (CC manages worktree contents natively; not source)
.claude/worktrees/
